- [x] Locate crash cause: `SavingsGoal` was used without `goals` in `Dashboard`.
- [x] Fix crash: pass `goals` into `SavingsGoal` from `Dashboard.jsx` (dashboard view).
- [x] Harden component: default `goals = []` in `SavingsGoal.jsx`.
- [x] Add UI to open “Add Goal” modal from `SavingsGoal.jsx` (button + modal).
- [x] Pass `setGoals` into `SavingsGoal` from `Dashboard.jsx` so modal can update list.

- [ ] Verify: dashboard shows Add Goal option and progress updates after adding a goal.

